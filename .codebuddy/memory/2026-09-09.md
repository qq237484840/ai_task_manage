# 2026-09-09 执行流水

## PM DoD 复核：Task-003 → APPROVED（2026-09-09）

- 依据：用户裁决（ask_followup，推荐项 APPROVED，同步范围=变更/任务单据）。
- 独立复核证据（复核时点实测）：pytest **89 passed**；`frontend/dist/` 产物在（vue-tsc 干净 + vite build 成功记录）；零构建 `app.js`/`styles.css` 已删、`index.html` 为 Vite 入口；`frontend/README.md` + 锁文件已落；GET / 托管 dist 200 + API 冒烟 19 步通过；M001 DESIGN/FILES/SUMMARY + ARCHITECTURE 回填抽查一致；无契约/后端业务改动；Task-002 未并行写 frontend。
- 落库文件（仅变更/任务单据，按用户所选范围）：
  - `docs/changes/CHANGE-002.md`：头部状态注明 DoD 已复核 APPROVED（2026-09-09）、DoD 六项勾选 [x]、处理路径更新、§复核记录（方式/结论/关闭条件）填写。
  - `docs/agents/Task-003.md`：状态 → 已完成、§6 DoD 全勾（项 7 = APPROVED + 关闭勾销留待 Task-002）、新增 §PM 复核记录。
- **遗留（非本次范围，未动）**：AGENT_REGISTRY / PROJECT_STATUS 等治理层索引未更新（用户本次仅选变更/任务单据，后续需要时补）。CHANGE-002 关闭条件挂起：待 Task-002 前端基础 UI 新栈实现并冒烟，于 Task-002 复核时由 PM 一并勾销。

## Task-002 后端编码（M002）：实现完成（2026-09-09）

- 产出 `backend/app/modules/m002/` 全链路：domain（models/enums/errors）、repository、services（quality/normalizer/image_store/upload/association/undo/photo_query/suggestion/dto_builders/locks）、api（upload/photo/association/content 路由）、clients/task_client（M001 只读桥接，越权转 404）、config（`AT_M002_*`）、schemas。
- 关键实现：上传两阶段（先文件后行，失败清理、无残留）；归属状态机（assign/confirm_suggestion/reject，rejected 不可再操作 409）；首张 assigned 幂等触发任务 in_progress；`services/locks.py` 键控互斥（D8：上传按 family+batch、归属按 family+task、删除按 photo_id）；`(batch_id, seq_no)` UNIQUE 兜底并发。
- 质检 v1.0 修复与校准：模糊 = **浮点 4-邻域拉普拉斯方差**（config 新增 `quality_blur_probe_side=320`，规避字节裁剪）；倾斜 = 文本行投影启发式；过暗/过亮/遮挡/页角裁切为确定性规则，阈值全配置化。合成图校准：清晰页 ~800+ vs 模糊 <200、rot20 判倾斜。
- 测试：`tests/unit/test_m002_image_processing.py`（11）+ `tests/api/test_m002_api.py`（12：双主体越权矩阵/归属状态机/失败无残留/受控取图）；**全量 pytest = 112 全绿**（M001 89 + M002 23，已计入 MEMORY 基线）。
- 文档回填（M002 九件套范围内）：MODULE.md 状态、MODULE_FILES.md（实况树）、MODULE_SUMMARY.md（状态/D8 锁语义/自检 112）、MODULE_CHANGELOG.md（Task-002 后端实现条目）。DATA/API/CONTRACT 未漂移未改。
- 清理：`_calib.py`、`_dbg_rot*.png`、临时 pytest 输出已删；`backend/data/images` 为运行时忽略目录。
- **遗留（非本次范围）**：Task-002 前端基础 UI（新栈 Vue3+TS+Vant4）待 Task-003 已批后接轨；顶层 AGENT_REGISTRY/MODULE_REGISTRY/PROJECT_STATUS/API_REGISTRY 状态由 PM 在模块 DoD 时统一更新；Task-002 任务书流转标记留待 PM。

## 图片孤儿积压根因与修复（2026-09-09 晚，用户问询）

- 现象：`backend/data/images/` 积压 30 个 UUID 目录 × 2 文件（原图 + `_n` 归一图，各 15624B/16402B 字节全同）。
- 根因：`tests/api/test_m002_api.py` 的 `m002_settings` fixture 把存储根重定向到 pytest tmp 目录，但**非 autouse**（仅 4 个用例引用）；其余执行成功上传的用例（photo_list_student_scope / assign_* / cross_family 等）无 override → 走默认 `backend/data/images`。DB 每用例独立 tmp 库，物理文件却无 GC，每次全量 pytest 追加 ~4 目录 → 多次运行累积 + 确定性合成图字节相同 = “大量重复图片”。文件路径全部经路由 `Depends(get_m002_settings)` 注入，DI override 即可全覆盖。
- 修复：fixture 改 `@pytest.fixture(autouse=True)`（带说明注释）；复跑全量 112 绿，运行前后 images 目录数不变（30→30）不再新增。
- 清理：已删除 30 个孤儿目录（0 残留）；`.gitignore` 已含 `backend/data/images/`（不入库，运行时数据）。

## Task-002 前端基础 UI（M002，新栈）：编码完成（2026-09-09 晚）

- 产出（`frontend/`）：
  - `api/types.ts`：新增 M002 DTO（Photo/PhotoStatus/QualityReport/AssignmentInfo/SuggestionInfo/ContentUrls/UploadBatch/UploadPhotoResult/AssociateParams…），`TaskItem` 补可选 `group_no`（CR-001，归属目标段推导用）。
  - `api/index.ts`：M002 客户端（createUploadBatch/uploadPhoto/listPhotos/associatePhoto/deletePhoto/fetchPhotoBlob——受控取图转 objectURL 供 `<img>` 展示）。
  - `utils/format.ts`：PHOTO_STATUS_TEXT 等映射。
  - 视图：`views/PhotoUploadView.vue`（建批次→van-uploader 多图→逐图质检结果/拒绝原因）、`views/PhotoListView.vue`（状态筛选/缩略图/preview/归属弹层【任务→学科段 chips 推导自任务 items subject+group_no】/采纳建议/驳回/删除）。
  - `router/index.ts`：`/photos`（bottom tab「照片」已加，BottomNav active 联合类型扩展）与 `/photos/upload`。
  - **修复既有缺陷**：`main.ts` 此前从未注册 Vant（各视图用大量 `van-*` 组件却无全局注册，运行时会解析为未知自定义元素）→ 已 `app.use(Vant)` 全量注册（ADR-012 本意）。
  - 路由守卫/双主体沿用 M001（session.isFamily 决定学生选择器显隐与 listTasks student_id 传参）。
- 验证：`npm run build`（vue-tsc --noEmit + vite build）**通过**（M001 各视图 + Photo* 新 chunk 产物正常）；lint 0。
- **遗留**：浏览器级冒烟（真机交互：上传→质检报告→归属全流程演示）与 M002 九件套/任务书回填留待下一段（PM DoD 复核前完成）；Node 22.22.2 实机路径 = `C:\Users\Administrator\.workbuddy\binaries\node\versions\22.22.2-2`（旧记忆缺 `-2` 后缀，已同步 MEMORY）。

## 根 README 补「快速开始」启动指引（2026-09-09）

- 用户要求将前后端启动脚本整理进 readme；仓库无任何 .sh/.bat/.ps1 脚本 → 直接在根 `README.md` 新增「快速开始（本地启动）」章节（开发分离/单进程/测试/端口表），并顺带修正过时的"待需求确认"头部状态句（改为指向 docs/PROJECT_STATUS 等）。
- 命令依据实况核对：后端 `.\.venv\Scripts\python.exe -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000`；前端 `npm run dev`（5173，/api 代理 8000）；生产=先 `npm run build` 再单启后端（AT_FRONTEND_DIR 覆盖 frontend/dist）；后端启动自动建库+seed，配置 AT_ 前缀/.env。
- 前端命令细节仍以 `frontend/README.md` 为权威（根 README 链接引用，避免知识重复）。

- MEMORY.md 已精简重写（去除与 docs/ 重复的流水叙事，收敛为当前指针 + 偏好 + 状态），原密集版本内容仍可从 2026-09-08.md 与 docs 还原。

## 验收启动：换端口双进程（2026-09-09 晚）

- 场景：8000/5173 被另一工程占用（8000 = Stock Forecast Quant API，5173 = React dev，均经 wslrelay 转发），用户选「换端口双进程」。
- 改动 `frontend/vite.config.ts`：port/代理目标改环境变量可覆盖（`VITE_DEV_PORT`、`VITE_API_PROXY_TARGET`，默认仍 5173/127.0.0.1:8000，README 标准用法不变）——本次验收默认用法的不变量保持。
- 常驻进程：后端 `uvicorn app.main:app --host 127.0.0.1 --port 8010`（venv python，pid 记录于 TEMP 日志 atm_uvicorn_8010.{out,err}.log）；前端 `node node_modules\vite\bin\vite.js --port 5174 --strictPort`（node 实机 22.22.2-2 绝对路径，env VITE_DEV_PORT/VITE_API_PROXY_TARGET，日志 TEMP atm_vite_5174.log）。
- 关键坑：Start-Process node.exe + RedirectStandardOutput 下 vite 无输出且不绑端口 → 改 cmd /c 批处理包装后正常（vite 只绑 IPv6 localhost，127.0.0.1:5174 连不上，须用 `http://localhost:5174`）。
- 探活：8010 openapi title=AI Task Management；localhost:5174 200 为本项目 Vue 页；`/api/v1/schools` 经代理 401（链路通）。预览已开 `http://localhost:5174/`。临时 bat 已删，日志留 TEMP。

## 验收冒烟：M002 全链路 live 通过（2026-09-09 晚）

- 针对运行中 8010 服务（真实 app.db + backend/data/images）做端到端脚本冒烟（httpx+PIL，账号 smokefam/smokekid 固定便于页面复核）。
- 全链路 PASS：family register/login → schools(北山小学 primary) → student 建(三年级) → 子账号开立 → task 建+publish → upload-batches → 上传 2 张合成清晰图（quality_passed=True, status=unassigned）→ 过暗图 422 image_quality_rejected（无残留已验证于测试）→ 列表 statuses → assign 归属 math/group0 → **任务推进 in_progress** → 学生会话列表可见本人 2 张 → 受控取图 200 image/jpeg → reject + delete → final 1 张 assigned；images 目录仅 1 个（原图+归一图）合理。
- 脚本小坑：httpx ok() 对图像 content 做 .json() 解析崩（服务无误，脚本误用），修正后收尾脚本跑通。临时脚本已删。
- 页面复核入口：`http://localhost:5174/` 登录 **smokefam / Passw0rd1**（子账号 smokekid）可见 SmokeKid 学生 + Smoke Math 20 任务（in_progress）+ 1 张 assigned 照片。
