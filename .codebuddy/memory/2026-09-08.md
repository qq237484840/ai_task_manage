# 2026-09-08

## 文档治理体系建立（Phase 0）

用户在空仓库 `c:\DevProject\AI.TaskManage` 中提供了一套「AI 原生多 Agent 软件工程总控 Agent」方法论（#0~#107），要求先建立文档体系，开发需求稍后提供。

### 用户决策（grill-me 确认，已落库）
1. 落地深度：分层渐进 —— 只建元治理层 + 项目级文档（15 份左右），模块 MODULE_* 九件套留到首次模块划分时生成
2. 语言：中文为主，代码/API 标识符保留英文
3. 占位：业务依赖文档结构建全 + TBD 标注 + 填充责任人
4. 工程骨架：只建 docs/，src/tests 等技术栈确定后再建
5. ID 治理：规范先行（ID_GOVERNANCE.md），暂不写校验脚本

### 产物（docs/ 共 16 个文件 + 根 README.md）
- 治理总览：README.md、docs/INDEX.md（知识地图）、docs/PROJECT_STATUS.md
- 项目级骨架（TBD）：PROJECT.md、SYSTEM_SUMMARY.md、ARCHITECTURE.md
- Registry（空表）：REQUIREMENTS.md、MODULE_REGISTRY.md、API_REGISTRY.md、DATA_MODEL.md、AGENT_REGISTRY.md（AGENT-001 Project Master 已启用）
- 规范：ID_GOVERNANCE.md、AGENT_GUIDE.md、DEVELOPMENT_GUIDE.md
- 记录：ASSUMPTIONS.md（ASM-001~006）、CHANGELOG.md（v0.1.0）、adr/ADR-001.md

### 约定
- 版本：当前 v0.1.0（文档骨架阶段）；PD-001~003 待决（业务范围/技术栈、领域与模块划分、语言选型）
- 计划中待创建文档（首条记录时生成）：TECH_DEBT.md、RISK_REGISTER.md、CONFIGURATION.md、EXTERNAL_SYSTEMS.md、DEPENDENCIES.md、FEATURE_MATRIX.md
- 模块 ID 格式 Mnnn、API ID 格式 API-Mnnn-nnn、REQ 需求单存 docs/requirements/REQ-xxx.md

### 下一步
- 等待用户提供开发需求 → 需求分析：需求分类、REQ 编号、影响分析、模块划分、Agent 分工、开发顺序、验收标准

## 治理模板包备份（同日）

用户要求把整套知识链做成可复用模板，供后续项目直接铺装恢复使用。

### 产物
- 位置：`c:\DevProject\AI.TaskManage\ai-governance-template\`
  - `README.md`（包说明 + 版本 v1.0.0）、`INSTALL.md`（铺装指南：复制 docs/ → 替换 {{占位符}} → 确认默认假设 → 进入正常运转）
  - `docs/`（16 个通用化文档模板，与本项目实例 docs 对应；本实例特化的日期/状态/假设/路径已参数化：{{PROJECT_NAME}}/{{PROJECT_DIR}}/{{INIT_DATE}}）
- 通用规则类（AGENT_GUIDE/DEVELOPMENT_GUIDE/ID_GOVERNANCE/各 Registry）与源实例一致；记录类（PROJECT_STATUS/CHANGELOG/ASSUMPTIONS/ADR-001）已通用化为 v0.0.1 起始。
- 项目实例 README 已加「治理模板包」段落指向模板。

### 约定
- 模板包为复用资产，不参与本项目知识分层；后续本仓库治理规范改进需回同步模板并递增版本。
- 占位符 {{...}} 铺装时必须全部消除；模板内不混入任何项目业务内容。

## V1 需求文档化（Phase 1，同日）

用户提供「V1 MVP 总控 Agent 指令」：**AI 每日作业智能评定系统**（中小学生 AI 作业与学习成长综合评定系统 V1）。范围严格限定 M01~M07 闭环：作业任务管理→图片采集→AI识别→任务匹配→质量评价→AI教师评价→今日报告。禁止知识图谱/画像/组卷/班级管理/多租户等。开发顺序强制、前序验收通过才进下一模块。

### 本次产出
- 项目级文档填充：PROJECT.md / SYSTEM_SUMMARY.md / ARCHITECTURE.md（V1 高层草案，技术栈标 PD）
- 需求：REQUIREMENTS.md 登记 REQ-001~008；docs/requirements/REQ-001~008.md（Draft 待确认）
- Registry：MODULE_REGISTRY（M001~M007，需求 M01~M07 别名映射）；AGENT_REGISTRY（AGENT-M001~M007）；DATA_MODEL（DATA-001~010）；API_REGISTRY（契约阶段登记）
- 记录：PROJECT_STATUS（v0.2.0 / Phase 1 / PD-001~008）；CHANGELOG v0.2.0；ASSUMPTIONS 更新（ASM-001/006 Rejected，新增 ASM-007~009）；RISK_REGISTER（RISK-001~005）；adr/ADR-002（V1 范围控制）、ADR-003（AI 评价边界与原则）
- 版本推进：v0.1.0 → v0.2.0

### 关键决策（供确认）
- 模块 ID 统一采用 Mnnn 三位格式：M01→M001 …
- PD-001~008 待用户确认（技术栈、身份模型、正确度判定基准、AI Provider 策略等），确认后才进入模块契约设计/编码

### 下一步
- 用户确认 PD → 输出模块契约设计计划（M001 起）→ M001 开发 → 逐模块验收推进

## 关键决策确认与落库（同日，v0.3.0）

用户确认 4 项关键 PD（grill-me 形式，一次提问全部答复）：
1. PD-003 → **ADR-004**：移动优先 H5 + Python FastAPI + SQLite 单体（浏览器相机拍照，图片本地受控目录）
2. PD-004 → **ADR-005**：纯家庭模式身份模型（家庭账号家长=布置者 + 学生档案=评定对象；家庭级数据隔离；无教师/班级）
3. PD-005 → **ADR-006**：正确度分科混合判定（客观题对照教师录参考答案判对/部分对/错；主观题不判对错只评完成质量并注明）
4. PD-006 → **ADR-007**：AI Provider 抽象 + Mock 先行（Vision/OCR/LLM 三类；本地无 key 跑通全链路；统一调用记录 DATA-009）

落库同步：PROJECT.md / SYSTEM_SUMMARY.md / ARCHITECTURE.md / DATA_MODEL.md（存储=SQLite+本地图片目录，DATA-002 收敛家庭账号+学生档案 Owner M001）/ ASSUMPTIONS.md（ASM-007/008 → Confirmed，新增 ASM-010/011 默认）/ REQUIREMENTS + REQ-001~008（状态行更新）/ RISK_REGISTER（RISK-001 Closed、002/004 Mitigating）/ PROJECT_STATUS（v0.3.0，PD-003~006 Confirmed）/ CHANGELOG / INDEX。

### 待复核（文本内已提请用户确认）
- ASM-010（PD-007 默认：单机/局域网部署、SQLite+本地图片目录、备份=库+图片目录整体备份）
- ASM-011（PD-008 默认：小学语数英书面作业起步、逐题标注学科/题型、无法判断→标记+家长复核/重拍）
- 复核后 REQ-001~008 转 Approved → 进入 Phase 2 模块契约设计（M001 起）与编码

### 用户决策复盘（长期适用）
- 确认交互形式：一次 ask_followup 多问（≤4 题），选项带推荐标注，用户逐项选择答复；答复后我按推荐/选择落库 ADR。

## 复核通过 + M001 契约草案（v0.4.0，Phase 2）

- 用户复核接受 **ASM-010/ASM-011** → Confirmed；REQ-001~008 → **Approved**（V1 需求基线冻结）；进入 Phase 2 模块契约设计。
- 产出 **M001 九件套契约草案** `docs/modules/M001/`（v0.1.0 Draft，待用户批准）：MODULE/MODULE_SUMMARY/MODULE_CONTRACT/MODULE_API/MODULE_DATA/MODULE_DESIGN/MODULE_FILES/MODULE_TEST/MODULE_CHANGELOG。
- M001 = 家庭空间 + 任务生命周期基座：REST API-M001-001~011（family 认证 3 + students 3 + tasks 5）+ 内部接口 FamilySpaceService/TaskQueryService/TaskStateService。
- 任务状态机：`draft --publish--> published --(M002 首传 mark_in_progress)--> in_progress --close--> closed --reopen--> published`；题目编辑仅 draft/published，in_progress 起冻结。
- 数据表：family_accounts/students/auth_sessions/tasks/task_items（映射 DATA-001/002，唯一写入口 M001，family_id 强制过滤）。
- 技术细节：SQLAlchemy 2.0 + Pydantic v2 + scrypt（标准库）密码 + 不透明 token 库存哈希；前端建议 Vue3+Vite 由 FastAPI 托管（编码开始时定）。
- 状态同步：PROJECT_STATUS v0.4.0 / MODULE_REGISTRY（M001 Designing）/ API_REGISTRY（API-M001-* Draft）/ INDEX / AGENT_REGISTRY / REQUIREMENTS+REQ×8（Approved）/ CHANGELOG。

### 下一步（用户批准 M001 契约签署区后）
- Project Master 签发 Task-001 → AGENT-M001 编码（建 backend/frontend 工程骨架 + 实现 M001 + 测试 + 回填九件套）→ DoD 验收 → 进入 M002 契约。

## M001 学校基础资料增补（同日，v0.4.1，Phase 2）

- 用户 M001 开发输入：需增加"学校基础资料"，学生档案须记录所在学校（原契约 `students.school` 仅自由文本可选）。
- 用户四项决策（ask_followup 确认）：① 学校字典=**全局共享只读** + 数据库 **seed 预置**（后台维护功能暂缓、不开发）；② 学生档案 **school_id 必填** 关联；③ 字段=名称+学段（primary/junior/senior 建议值，V1 起步 primary）；④ 消费范围仅 M001（M007 报告展示学校名留待其契约）。
- 落库：**REQ-009**（学校基础资料，Approved，承载于 M001）；**ADR-008**（Accepted：schools=全局公共只读，ADR-005 家庭隔离的限定例外；学校管理域/班级/多教师仍禁止）；**DATA-011**（schools）；**API-M001-012**（GET /schools 只读列表）。
- M001 契约 v0.1.0 → **v0.1.1**：`students.school` 文本 → `school_id` FK NOT NULL；schools 表 seed 幂等初始化、运行期无写路径；九件套全部同步；MODULE_CHANGELOG +v0.1.1。
- 全局同步：DATA_MODEL（DATA-011 + ownership 例外）、REQUIREMENTS（+REQ-009；顺带修复 REQ 状态列 Draft→Approved 漂移）、MODULE_REGISTRY / API_REGISTRY、PROJECT_STATUS → v0.4.1（+PD-009 行）、CHANGELOG v0.4.1、PROJECT.md / ADR-005 措辞澄清（学校管理域禁止不变）。
- 当前 M001 契约草案 v0.1.1 仍待用户批准签署区 → 批准后签发 Task-001 编码。

## M001 契约批准 → v0.5.0 / Task-001 签发（同日）

- 用户回复「确认批准」→ M001 契约基线 **v0.1.1 Frozen**：`MODULE_CONTRACT.md` 签署区记录 用户批准 + PM 批准（2026-09-08）。
- 落库：M001 九件套头部状态（Contract/API/Data→Frozen，MODULE.md→Developing，DESIGN/FILES/TEST→实现中/已批准）；`API_REGISTRY`：API-M001-001~012 Draft→**Frozen**（改须 CR）；`MODULE_REGISTRY`：M001 Designing→**Developing**；`AGENT_REGISTRY`：**AGENT-M001 Planned→Active（Task-001）**，AGENT-001 任务=监督 M001 开发与 DoD 验收；`PROJECT_STATUS` → **v0.5.0**（开发中模块 M001；下一步行动更新）；`CHANGELOG` v0.5.0；`MODULE_CHANGELOG` +v0.1.1(Frozen) 批准行。
- **Task-001（AGENT-M001）**：backend/frontend 工程骨架 + M001 功能实现 + 测试 + 回填九件套实现版（按 AGENT_GUIDE §4/§6）。编码时注意：schools seed 幂等、school_id 必填、family_id 强制过滤、状态机/冻结规则、Frozen 契约变更须 CR。
- 下一步：AGENT-M001 编码 → PM 按 DoD 验收 M001 → 通过后进入 M002 契约设计（M001→…→M007→全系统回归）。

## Task-001 实现完成（同日，v0.6.0）

- **AGENT-M001 完成 Task-001**：M001（家庭空间 + 任务生命周期）后端/前端/测试全部落地，**54 passed**（8.08s）；真服务冒烟 GET /、/styles.css 200。
- 工程落地：`backend/`（FastAPI + SQLAlchemy 2.0 + Pydantic v2，`app/`：core/shared/api/v1/modules/m001/models|repositories|services|schemas）；`frontend/`（**零构建原生 H5**：index.html/styles.css/app.js，FastAPI 静态托管 —— 决策时点本机无 Node，REST 契约不受影响，Vue3+Vite 仅作可替换壳）；`backend/tests/`（unit×3 + integration×1 + api×5）；`backend/.venv`（Python 3.12）。
- 关键实现确认：请求级统一 commit/rollback；`_UNSET` 哨兵实现 PATCH"显式清空 vs 未提供"；SQLite `PRAGMA foreign_keys=ON`（event 钩子）；学校 seed 幂等 + 独立空库验证；`_TRANSITIONS` 表驱动状态机（非法边 409）+ `mark_in_progress` 幂等；REST 越权对外 404（内部接口 403 PermissionDenied 语义分化）；审计日志不含密码/token/答案；测试 conftest 双家庭 fixture + scrypt 降参加速。
- 文档回填（九件套实现版）：MODULE.md/SUMMARY（Testing）、DESIGN（+实现期决策记录 10 条，唯一自适应调整=前端零构建 H5）、FILES（L6 实况清单）、TEST（清单逐项勾选 + 54 passed）；同步 MODULE_REGISTRY（M001 Testing）、AGENT_REGISTRY、PROJECT_STATUS（→**v0.6.0**）、CHANGELOG v0.6.0、MODULE_CHANGELOG。
- **待办（Project Master）**：按 AGENT_GUIDE §6 DoD 验收 M001 → 通过转 Stable → 进入 M002（作业图片采集）契约设计。

## M001 验收通过 + M002 契约草案（同日，v0.7.0→v0.7.1）

- **用户：验收通过** → PM 按 DoD（AGENT_GUIDE §6）验收 **APPROVED**：M001 Testing→**Stable**（v0.7.0；契约 v0.1.1 Frozen 不变，API-M001-001~012 未越契约）。同步 MODULE.md/SUMMARY（Stable）、MODULE_REGISTRY、AGENT_REGISTRY（AGENT-M001 Task-001 APPROVED）、PROJECT_STATUS→v0.7.0、CHANGELOG v0.7.0、MODULE_CHANGELOG（+验收行）、**INDEX（修复 v0.4.1~v0.6.0 期间漂移）**。
- **M002 四关键决策用户确认（均选推荐项，PD-010~013）**：
  - D1/PD-010：质量检测=**本地规则先行**（模糊/过暗过亮/倾斜/遮挡/缺页启发式可解释判定 + 阈值配置化 + 规则版本 v1.0，无外部 AI；QualityChecker 协议预留 Vision 接入位）
  - D2/PD-011：预处理=**仅轻量归一**（EXIF 方向归一 + 统一 JPEG + 长边 ≤2000px；透视矫正/增强归 M003 识别前链路）
  - D3/PD-012：提交建模=懒创建/自动归属 open 提交 + 显式 complete（open→completed 幂等）；**任务首张质检通过入库即 mark_in_progress**（published→in_progress，幂等，可继续上传至任务 close）
  - D4/PD-013：不合格=**不入库 + 逐图报告**（失败不留文件/行/状态；422 `image_quality_rejected` 引导重拍）
- **M002 九件套契约草案 v0.1.0（Draft）产出** `docs/modules/M002/`：提交模型 submissions（seq_no 按 task+student 递增）+ submission_images（**family_id 冗余防御列**、original/normalized 相对路径、sha256、quality_report_json）；REST API-M002-001~004（POST /submission-images 上传、GET /submissions、POST /submissions/{id}/complete、GET /images/{id}/content 受控取图+Range+访问审计）；失败图不入库；图片目录 `<backend>/data/images` 分层、不挂公开静态、鉴权读取；M003 消费内部 SubmissionQueryService。
- 全局同步 v0.7.1：API_REGISTRY（+API-M002-001~004 Draft）、MODULE_REGISTRY（M002 Designing/Draft）、AGENT_REGISTRY、PROJECT_STATUS（+PD-010~013）、CHANGELOG v0.7.1、INDEX、MODULE_CHANGELOG（M002 v0.1.0 行）。
- **待办**：用户批准 M002 契约 v0.1.0（签署区 MODULE_CONTRACT.md）→ Frozen → 签发 Task-002（AGENT-M002）编码 M002。

## M002 契约完整性审查 → 两项架构转向 + 一次落库（同日，v0.8.0）

用户以 grill-me 方式要求审查"当前规划需求的完整性"并逐项确认 → 揭示真实产品闭环与既有 M001/M002 基线存在**两项架构级转向**，经 4 轮逐项确认（均记录 PD/ADR/CR）后按授权一次落库：

### 转向一：任务 = 多学科作业登记单容器（PD-014 → CR-001）
- 用户口径：一份作业登记单可登记多学科内容（逐项标记学科）；拍照批量上传不选学科，AI 自动识别归属到对应任务/学科作业段，无法识别由家长手动关联兜底。
- 落定：task=容器（subject 语义放宽，`CR-001` Open）；`task_items` 题目级 + `group_no` 学科作业段（同科多份可区隔）；学科卡=`(subject,group_no)`；参考答案逐题对照保留（ADR-006）。

### 转向二：两级主体身份 = 家庭账号（家长）+ 学生子账号（PD-016 → ADR-009 Accepted + ACR-001 Open）
- 学生可独立登录（完整账号绑定 student_id，仅本人数据），自主登记/拍照；家长全家管理+兜底。ADR-005 家庭级隔离不变，措辞扩展。

### 采集模型（PD-015，M002 v0.1.0 → v0.2.0）
- **先采后认**：上传批次不预选任务/学科 → `unassigned` → AI(M003)建议 `suggested` → 家长/学生 `assigned`(到学科作业段三元组)/`rejected`；首次 assigned 触发 `mark_in_progress`（幂等）；完成程度=作业段照片覆盖二值化。
- 补全 D5~D8：批次≤50/任务 assigned≤200（配置化）；页序服务端自增；`consumed_at IS NULL` 可撤销（物理删+审计）；`(family_id,batch_id)` 串行化+409 `concurrent_conflict`。
- 数据：`upload_batches` + `photos`（归属状态机）；API-M002-001~006 重构登记。

### 落库清单（v0.8.0）
- 新增：`docs/adr/ADR-009.md`；`docs/changes/CR-001.md`、`ACR-001.md`（Open）；RISK-006/007。
- M002 九件套整体重构 v0.2.0；API_REGISTRY/MODULE_REGISTRY/PROJECT_STATUS（v0.8.0，PD-014~016+跨模块开放项 O-1~O-8）/CHANGELOG v0.8.0/REQUIREMENTS+REQ-001~003 精校/ADR-005 扩展注。
- **待办（重呈用户）**：批准 ①ADR-009（事实已落）②CR-001+ACR-001（→M001 变更执行）③M002 v0.2.0 签署区 → 之后签发 Task-002。开放项 O-1~O-8 随 M003~M007 契约轮关闭（含：照片归属建议契约/消费时机、报告时间窗需再对 M001 走 CR、DATA_MODEL/INDEX 字段级同步在 CR-001/ACR-001 批准后统一执行）。

## 核心需求 grill 定稿 → 主线功能域重排 + 内容级重构（同日，v0.9.0）

用户 grill-me 再次确认 6 条核心需求并按推荐项逐项定稿（8 项决策：g1~g4 + 内容级四问 q2-1~q2-4），全部落库为 PD-017~024：

### 8 项定稿（与既有基线的主要差异）
1. **g1/PD-017 布置登记录入=拍照识别为主**：拍黑板/记事本/布置页 → 大模型识别"今日作业清单（学科+条目）"草稿 → 学生/家长确认补正后生效为登记单；**M003 增加"布置单识别"AI 环节**（新 AI 类型，不再纯手工登记）
2. **g2/PD-018 升级内容级**：布置精确到题；匹配=照片内容↔布置题逐题对齐判完成/对错；大模型综合评分 → 重开 ADR-006
3. **g3/PD-019（需求⑤）真实三方默认运行**：OCR/Vision/LLM 真实三方为默认实现，Mock 降级为测试桩/离线降级（`mock-*` 标注）；部署需联网 → **ASM-010 修订**
4. **g4/PD-020 主线按功能域重排**：保留 M001~M007 模块组织与契约治理（开发顺序不变）；主线/验收里程碑=功能域 A~D → **新建 `docs/ROADMAP.md` v0.9.0**（A 学生自主采集→B 家长复核确认→C 大模型匹配+兜底→D 综合评判+报告；横切=真实三方 AI/MVC/两级主体；里程碑 M-A~M-D+M-END）
5. **q2-1/PD-021 判定基准=模型端到端直判**：不维护参考答案字段；大模型综合题干+作答判对错并给可观察依据（数学可验算/语文可比对原文），低置信入复核
6. **q2-2/PD-022 主客观全判**：主观题也判对错（全面重开 ADR-003/006 边界）；不武断/可观察依据/置信度/无法判断出口保留 → ADR-010
7. **q2-3/PD-023 M005 保留六维但 AI 评判为核**：逐题完成/对错 + 大模型综合评判（六维+综合分）基于识别/匹配证据，权重配置化不变
8. **q2-4/PD-024 报告草稿→家长复核定稿**：草稿→家长复核（低置信/无法判断/对错异议逐项过）→定稿归档（草稿+复核记录保留）

### 落库清单（v0.9.0）
- **新增**：`docs/ROADMAP.md`（功能域主线）；`docs/changes/CR-002.md`（内容级判定链数据面，Open）、`ACR-002.md`（判定+Provider 架构重构，Open）；`docs/adr/ADR-010.md`（内容级主客观全判+端到端直判，取代 ADR-006，Proposed）、`ADR-011.md`（真实三方默认，取代 ADR-007，Proposed）
- **修订**：ADR-006/007 → **Superseded**（附取代说明）；ASSUMPTIONS ASM-008/010/011（ASM-010=需联网，离线显式切 Mock）；RISK_REGISTER（RISK-002/003/004/006 口径升级 + **RISK-008 三方依赖、RISK-009 照片出域三方合规**）；DATA_MODEL（DATA-001 判定基准语义、DATA-004/005/006 内容级口径）；PROJECT_STATUS→**v0.9.0**（PD-017~024 + O-9 新增、下一步=4 变更批准包）；MODULE_REGISTRY/AGENT_REGISTRY（M001~M007 行口径）；CHANGELOG v0.9.0；INDEX（+ROADMAP/ADR-010/011/CR-002/ACR-002）；REQUIREMENTS（REQ-001~007 随 CR-002 精校预告）
- **批准点（待用户）**：① CR-001+ACR-001（v0.8.0 遗留）② **CR-002+ACR-002（本批新增）** → 生效后 ADR-006/007→Superseded、ADR-010/011→Accepted → M001 变更执行（合并 CHANGE）→ M002 契约升级 v0.3.x 批准 → ROADMAP 里程碑 M-A~M-D 依序推进（M001 变更→M002→M003…M007）→ 全系统回归（真实三方+Mock 双跑）

## 批准包全部批准 + M002 契约 v0.3.0 起草（同日，执行批）

- **用户审阅批准四项（均选推荐）**：CR-001（M001 容器化+group_no；附带条件=条款按 ADR-010 口径修订、组号默认语义实施收敛回填）、ACR-001（两级主体）、CR-002（内容级判定链数据面；批准后即起草 M002 v0.3.x）、ACR-002（判定+Provider 重构；顺手修订 ADR-010 末句引用瑕疵）。
- **落库状态迁移**：CR-001/ACR-001/CR-002/ACR-002 → **Approved**；ADR-010/011 → **Accepted**（ADR-006/007 Superseded 正式生效）；CR-001 草案 `reference_answer` 条款按 ADR-010 修订（保留为非基准辅助字段、"逐题对照能力"作废）；ADR-010 §Decision 评分消费句由"依 ADR-011"勘误为"依 q2-3/CR-002 语义"。全局同步：PROJECT_STATUS（阶段/模块表/PD-014~016 行/开放项 O-8/O-9/下一步行动）、MODULE_REGISTRY、API_REGISTRY、INDEX、REQUIREMENTS、AGENT_REGISTRY、CHANGELOG（+批准包节）、ARCHITECTURE、SYSTEM_SUMMARY、ROADMAP。
- **M002 契约九件套 v0.2.0 → v0.3.0（Draft，下一批准点）**：核心变更=移除"完成程度=照片覆盖二值化"，完成程度与对错改由内容级判定链（M003/M004，ADR-010）回写；M002 职责收敛=采集/质检/归一/归属/证据供给（R4）；AI Provider 按 ADR-011 真实三方默认、Mock 测试桩/离线（R5）；证据消费 mark_consumed 保留（R6）。MODULE_SUMMARY +R4~R6 决策表；MODULE_CHANGELOG +v0.3.0；先采后认/质检 D1~D8 均保留；表结构与 API-M002-001~006 不变。
- **待办（用户下一批准点）**：批准 M002 v0.3.0 签署区（MODULE_CONTRACT.md）→ Frozen → M001 合并 CHANGE 执行（CR-001/CR-002/ACR-001/ACR-002）→ 依 ROADMAP M-A~M-D 签发模块任务。

## M002 v0.3.0 批准冻结 + Task-002 签发 + CHANGE-001 立项（2026-09-08 执行批 2）

- **用户批准 M002 v0.3.0 签署区**（MODULE_CONTRACT）→ 九件套冻结：MODULE.md Developing、CONTRACT/API/DATA Frozen、DESIGN/FILES 实现中、TEST 已批准基线；MODULE_CHANGELOG +批准冻结行；签署区记录用户+PM 批准。
- **顶层同步 → v0.10.0**：PROJECT_STATUS（阶段=M002 开发+M001 变更执行、M002 Developing、下一步行动 5/6 更新）、MODULE_REGISTRY（M002 Developing/Frozen、M001 变更执行中）、API_REGISTRY（API-M002-001~006 Draft→**Frozen**）、AGENT_REGISTRY（AGENT-M002 Active/Task-002、AGENT-M001 Task-CHG）、INDEX、CHANGELOG（+v0.10.0）、ROADMAP 注（CHANGE-001 执行中、M002 Frozen）。
- **Task-002 签发**：AGENT-M002 Active —— 实现 M002 v0.3.0（backend `modules/m002` + tests + 回填九件套），依赖 M001 CHANGE 落地。
- **CHANGE-001 立项（Executing，AGENT-M001）**：`docs/changes/CHANGE-001.md` 合并 CR-001/CR-002/ACR-001/ACR-002 执行清单+验收标准；数据面 `task_items.group_no`+容器语义+冻结、认证面两级主体（学生子账号表+会话双型+越权矩阵）、reference_answer 非基准、54 测试回归+新用例、M001 九件套回填同步 DATA_MODEL。
- **M001 代码现状（已勘察）**：backend/app/modules/m001（orm/repos/services/schemas）、api/v1（family/students/tasks/schools）、shared/auth（AuthContext 仅 family）、deps/main（app 装配+建表 seed）；tests=conftest 双家庭 world + 4 api + integration。
- **待执行（下一开发段，无批准点）**：CHANGE-001 编码（M001 变更）→ 回填 → Task-002（M002 实现）→ ROADMAP M-A 里程碑 PM 验收。
