# 2026-09-16

## `CR-005` / `CR-006` 批准 → 契约修订 + 签发 `Task-018`~`Task-021`（用户：「批准」）

- **用户批准**：`CR-005`（布置单图片解析通路）+ `CR-006`（A/B/C 三项全批）。PM 解读为整体批准（若仅部分批准需用户纠正）。
- **契约修订完成（`CR-005` §6 第一步）**：
  - `docs/modules/M001/MODULE_API.md` → **v0.3.0**：新增 `API-M001-022`（`POST /api/v1/tasks/{task_id}/reparse`）**详细契约**（含 §2.4 三情形：`placeholder` 写入 / `parsed` 整体替换 / 失败**不清空**；`confirmed` → `409 spec_confirmed`；失败不抛错）；新增内部回调槽「布置单图片源读取（M001 定义 / M002 注册实现）」；申请清单加 `M001-A5`；端点表 + 说明同步。
  - `docs/modules/M002/MODULE_API.md` → **v0.4.2**：内部服务接口 **+1** `provide_task_source_image(family_id, photo_id) -> TaskSourceImage | None`（`{mime, abs_path}`，归属校验、**路径不外发**）。
  - `docs/API_REGISTRY.md`：登记 `API-M001-022`（**Draft**，随实施转 Active）+ 顶部 2026-09-16 注释。
  - `docs/MODULE_REGISTRY.md`：M001 → **v0.3.0**、M002 → **v0.4.2**。
- **签发 4 份任务书**（`docs/agents/`）：
  - `Task-018`（`AGENT-M002`）：`provide_task_source_image` + `ensure_task_spec_image_provider_registered`（幂等 + 自愈 + **M001 未就绪不阻断**）+ 导入期注册 + `main.py` 启动期 **1 行**自愈；写区 = `m002/clients/task_client.py`、`main.py`、新增用例。
  - `Task-019`（`AGENT-M001`）：回调槽（与 `register_links_migration_hook` 同模式）+ `_to_ai_sources` 图片源转发 + `TaskService.reparse` + `API-M001-022` 路由 + **7 例（含防伪造红→绿）**；写区 = `task_parser.py` / 新 `image_provider.py` / `task_service.py` / `routers/tasks.py` / 用例。
  - `Task-020`（`AGENT-M001` 前端）：任务详情页「重新解析」按钮（`confirmed` 隐藏/禁用）+ `reparseTask`（可选 `timeoutMs=60000`，**不改** `http.ts`）+ **如实反馈**（无内容项不得谎报成功）。
  - `Task-021`（`AGENT-M002` 或 PM）：**只读验收** —— 8 条清单（纯图片 reparse 真实 Vision 产草稿 + DATA-009 `mock=0` / **防伪造红→绿** / `409` / §2.4 三情形 / provider 归属校验 / 全量回归 / 前端类型检查与构建 / 浏览器级），证据**分域**。
- **关键判据（已写入 `Task-019`）**：图片源转发**仅当 Vision Provider 为真实** —— 判据取 `core/ai/providers/registry.py::ResolvedProvider`（`degraded is False` **且** `reason == "real"`）；**禁止硬编码模型名**；Mock / degraded / 槽未注册 / 取图失败 → **不转发**（保持 `placeholder`，**不伪造** —— `BUG-004` 教训）。
- **`CR-006` 已批准待排期**（A：`photos` 加 `belong_date`/`group_key` + 窗口过滤；B：`link-suggestions` 新增 `last_attempt` 读 DATA-009；C：`POST /photos/{id}/link-suggestions/retry` → `202` + 轮询）。**须在 `CR-005` 任务链之后串行**（与 M002 `photo_repository`/`link_routes`/`schemas` 同文件冲突）。
- **下一步**：实施 `Task-018`（M002 提供方）→ `Task-019`（M001 消费 + 端点）→ `Task-020`（前端）→ `Task-021`（验收）；完成后 PM 置 `API-M001-022` → Active、`CR-005` → `Applied`。

## 实施进展（同日）：`Task-018` 完成 —— M002 受控提供布置单图片

- **实施期 PM 裁决（契约修正）**：provider 原签名 `(family_id, photo_id)` **补入 `session`** → `(session, family_id, photo_id)`。依据 `app/core/database.py:3-5`「**不在此处持有隐式全局库连接**」（引擎/会话仅来自 `create_app` 或测试 fixture）→ provider 无法自建会话；且与 M001↔M002 既有内部接口惯例一致。已同步修订：`docs/modules/M002/MODULE_API.md`（内部接口表）、`docs/modules/M001/MODULE_API.md`（回调槽说明）、`docs/changes/CR-005.md §2.3`（+「实施期修正」记录）、`Task-018 §3.1`、`Task-019 §3.2`。属**非破坏性签名完善**（契约刚发布、尚无消费者）。
- **代码（写区：`m002/clients/task_client.py` + `main.py` + 新用例）**：
  - `TaskSourceImage{mime, abs_path}`（frozen dataclass）；
  - `provide_task_source_image(session, family_id, photo_id) -> TaskSourceImage | None`：`PhotoRepository.get_by_id` **归属校验** → `ImageStore(get_m002_settings().image_root).abs_path(photo.normalized_path)` → 存在性校验；查无/异常 → `None` + `logger.warning`；**只读**、不改状态、不写审计；
  - `ensure_task_spec_image_provider_registered()`（与 `ensure_links_migration_hook_registered` 逐字对齐：幂等短路 / 自愈 / **M001 未就绪 → `False` 不抛不阻断**）+ 导入期自动注册 + `main.py` 启动期 1 行自愈（紧邻既有 hook 自愈）。
- **用例（`tests/unit/test_m002_task_source_image.py`，6 例）**：正常受控路径（在受控根内）/ **跨家庭 → `None`** / 未知照片 → `None` / 文件缺失 → `None` / 注册幂等 + 自愈 / M001 未就绪 → `False` 不抛。
  - **调试坑（可复用）**：直接插 `photos` 行会 `FOREIGN KEY constraint failed`（缺 `upload_batches` 行）→ 修正为**批次经真实 API** `POST /api/v1/upload-batches` 创建，照片行走真实仓储 + `ImageStore` 落真实文件；**未**用桩绕过 FK 链路。
  - 「幂等/自愈」用例以 `sys.modules` 注入**假 M001 `image_provider` 模块**验证回调契约（docstring 已报备）；建议 `Task-021` 补真实槽断言。
- **验证**：新增 6 例 `EXIT=0`；**全量 `tests=257 failures=0 errors=0 skipped=0`、`PYTEST_EXIT=0`**（基线 251 + 6）；写区双证成立（`task_client.py`/`main.py` 为写区；`m001/services/task_parser.py`、`core/ai/errors.py` mtime 仍是 09-14 15:48:34 → **零写入**）；`read_lints = 0`。
- **当前状态提示**：M001 侧 `image_provider` 尚未创建（`Task-019`）→ 注册返回 `False` + warning，**属预期、不阻断**；`Task-019` 完成后自动生效。
- **下一步**：`Task-019`（M001 回调槽 + `_to_ai_sources` 图片源转发 + `API-M001-022` 端点 + 7 例含防伪造红→绿）→ `Task-020`（前端）→ `Task-021`（验收）；完成后 `API-M001-022` → Active、`CR-005` → `Applied`。

## 实施进展（同日）：`Task-019` 完成 —— M001 图片源转发 + `API-M001-022` 重新解析

- **写区更正（PM 裁决）**：M001 路由**不在** `modules/m001/routers/`（该目录不存在），实际在 **`app/api/v1/tasks.py`**（见 `app/main.py:56`）→ 已更正 `Task-019` 写区条目。
- **代码**：
  - **新增** `m001/services/image_provider.py`：回调槽 `TaskSourceImage{mime, abs_path}` + `register_task_spec_image_provider(fn|None)` + `get_task_source_image(session, family_id, photo_id)`（未注册/实现抛错 → `None`，不阻断）。
  - `m001/services/task_parser.py`：`Parser` Protocol 增 `family_id`；**新增 `_vision_is_real()`**（判据 = `get_ai_service().providers.vision` 的 `reason=="real" && !mock && !degraded`；**禁止硬编码模型名**；不可读 → False 保守不转发）；`_to_ai_sources(sources, *, session, family_id)` 支持图片源（**三条件齐备才转发**：判据为真 + session/family_id 可用 + 槽已注册且取图成功）；`default_parser(..., family_id=None)` 透传。
  - `m001/services/task_service.py`：`ingest` 解析调用补 `family_id`；**新增 `reparse()`**（`confirmed` → `ConflictError`(409)；`placeholder` → 写入 → `parsed`；`parsed` 未确认 → **整体替换**；无草稿/失败 → **不改动不清空**；审计 `task_reparsed`）。
  - `app/api/v1/tasks.py`：**新端点 `POST /api/v1/tasks/{task_id}/reparse`**。
  - **新增** `tests/api/test_task_reparse_api.py`（**8 例**）。
- **红→绿（防伪造判别力，PM 亲手）**：临时移除 `_vision_is_real()` 判据 → `test_no_forgery_when_vision_mock_even_if_provider_registered` **FAILED**，审计日志 `task_reparsed … parsed=True replaced=True spec_status=parsed` → **Mock 立刻产出「假解析」**（`RED_EXIT=1`）→ 还原 → 全绿。**证明该判据是真防线**（不只是「槽未注册所以没转发」）。
- **验证**：新增 8 例全绿；**全量 `tests=265 failures=0 errors=0 skipped=0`、`GREEN_EXIT=0`**（基线 257 + 8）。
- **调试记录（可复用）**：① 用**图片源**建任务时 `ai_sources` 为空 → 假 parser 不会被调用（`default_parser` 里 `parser = _ai_parser() if ai_sources else None`）→ 构造 `placeholder` 用例应改用**文本源 + 先 `real`+禁兜底**；② `ImageInput` 字段名是 **`path`**（不是 `abs_path`）；③ `Task-018` 的**假 M001 模块**用例在真实 `image_provider` 出现后失效（`from package import name` 会命中包属性上的真实模块）→ 改为**真实槽位断言**（顺带**去掉一个桩**）。
- **台账**：`API_REGISTRY` / `M001 MODULE_API`：**`API-M001-022` → Active**（2026-09-16，实施 + 8 例 + 防伪造红→绿）；`CR-005` 状态进度更新（`Task-018` ✅ / `Task-019` ✅ / `Task-020` ⏳ / `Task-021` ⏳）。
- **下一步**：`Task-020`（前端「重新解析」入口 + 60s 单请求超时 + 如实反馈）→ `Task-021`（跨模块真机 + 浏览器级只读验收）→ `CR-005` → `Applied`；随后 `CR-006`（A → B → C）。

## 实施进展（同日）：`Task-020` 完成 —— 任务详情页「重新解析」入口（真实 Vision 正向验证通过）

- **代码**：
  - `frontend/src/api/index.ts`：新增 `reparseTask(task_id, options?: { timeoutMs? })` → `POST /tasks/{task_id}/reparse`（**仅传参时**覆盖单请求 timeout；**全局 `http.ts` 未动**）。
  - `frontend/src/views/TaskDetailView.vue`：动作区新增「**重新解析**」按钮（`v-if="canReparse"` = `placeholder`/`parsed`；`confirmed` **隐藏**）+ 解析中提示「AI 解析中，约 15–30 秒…」；`reparse()` 按 `spec_status === "parsed" && contents.length > 0` 判定成功文案，否则提示「AI 未返回解析结果…（可稍后重试或手工补录）」；异常 `toastError` + `load()` 刷新（409 场景）。
- **验证（分域，全部实测）**：
  - 前端静态级：`npx vue-tsc --noEmit -p tsconfig.app.json` → **`TSC_EXIT=0`**
  - 构建级：`npm run build` → `TaskDetailView-7e3MOpRs.js 8.17 kB`、**`BUILD_EXIT=0`**
  - **浏览器级 7/7 PASS（真实三方 AI，8010）**：`布置单照片上传（kind=task_spec）201` → **`setup: ingest spec_status=parsed contents=6`** → 「重新解析」按钮出现 → 点击 → toast **「已解析出 6 项内容，请到「草稿确认」核对」** → **成功文案与真实 `contents=6` 一致（不谎报）** → 页面「内容项（6）」与接口一致 → 无 favicon 404 之外 JS 错误 / 无 4xx 资源。
  - **重大副成果：布置单「图片源」解析真实打通** —— `POST /tasks` 带 `kind=image` 源 → 建任务**即 `parsed` 并产出 6 项内容** ⇒ `Task-018`（M002 受控取图）+ `Task-019`（M001 图片源转发）的跨模块通路在真实 Vision 下端到端生效（`REQ-011` 第二阶段 A1 的核心目标达成）。
- **调试坑（可复用）**：Playwright 用 `.card` + `hasText: "内容项"` 会命中**任务概要卡**（其含「内容项 N 项」字样）→ 应用 `h3.section-h` 精确定位；**属脚本定位问题，非产品缺陷**（如实记录于 §5⑦）。
- **清理**：`.e2e/_tmp_t020.mjs`、4 张截图、`backend/data/_tmp_t020.jpg` 全部删除；8010 服务保持运行（PID 50740，Health OK）。
- **下一步**：`Task-021` 验收（8 条清单：真实 Vision 产草稿 + DATA-009 `mock=0` / **防伪造红→绿** / `409` / §2.4 三情形 / provider 归属校验 / 全量回归 / 前端类型检查与构建 / **浏览器级（建议补失败分支）**）→ 通过后 `CR-005` → `Applied`；随后 `CR-006`（A → B → C）。

## `Task-021` 验收完成 —— `CR-005` → `Applied`（8 条清单全通过）

- **真机取证（真实服务 8010，真实 Vision）**：
  - **#1**：布置单图片源 `POST /tasks → 201 / spec_status=parsed / 4 项内容`（`math | 练习册 P23 第1-10题`、`1. 12 x 4 = 48`、`2. 96 / 3 = 32`、`3. 7 + 8 = 15`）；**DATA-009 原文** `capability=task_spec_parse / model=qwen3.8-flash / status=ok / mock=0 / latency_ms=16223`。
  - **#3**：`confirm=200` → `reparse=409`，`body={"code":"CONFLICT","message":"任务解析已确认（spec_confirmed）"}`。
  - **#5（服务级，关键发现）**：**家庭 B 引用家庭 A 的 `photo_id` 建任务 → `201`**（M001 **不校验归属**）但 `spec_status=placeholder / contents=0`，`reparse` 仍 `placeholder/0` → **越权实际由 M002 provider 的 `family_id` 过滤兜住**（不转发 = 不取图）。
- **#2 防伪造红→绿（PM 亲手）**：临时把 `task_parser.py:220` 的 `if not _vision_is_real(): continue` 改为 `if False:` → `FAILED test_no_forgery_when_vision_mock_even_if_provider_registered`：`assert 'parsed' == 'placeholder'` + 审计 `task_reparsed … parsed=True replaced=True spec_status=parsed`（**Mock 占位草稿冒充解析结果**，`BUG-004` 教训复现）→ `RED_EXIT=1`；还原 → 聚焦 16 passed / 全量绿。**另 3 例仍 PASS** → 证明「归属校验」与「真实性判据」是两个独立防线。
- **#4/#5 用例级**：#4 三情形（`placeholder` 写入 / `parsed` **整体替换** / 失败**不清空**）由 `tests/api/test_task_reparse_api.py` 覆盖；**新增** `tests/integration/test_cr005_image_source_service.py`（2 例，**真实 provider + 真实 `PhotoRepository`/`ImageStore`**：跨家庭 → 观测桩 `sources` 无 `image`；本家 → `sources == ['image']` 且 path 在受控根内）。
- **#6/#7**：全量回归 **`tests=267 failures=0 errors=0 skipped=0`、`GREEN_EXIT=0`**（265 + 新增 2）；前端 `TSC_EXIT=0` / `BUILD_EXIT=0`。
- **#8 浏览器级**：成功分支 7/7（`Task-020`，真实 Vision）；**失败分支 7/7（本次构造）**：临时 `AT_AI_PROVIDER_MODE=mock` → 图片源不转发 → 点击「重新解析」→ toast **「AI 未返回解析结果（可能暂时不可用或图片无法识别），可稍后重试或手工补录」**、接口仍 `placeholder/0`、页面「内容项（0）」→ **未谎报**；随后**恢复 `real` + 重启**（PID 15172，Health OK）。
- **登记 `TD-006`**（**注意：`TD-005` 已被占用** —— 已存在「未挂接照片无窗口归属」，见 `TECH_DEBT.md:52`；新 ID 由 PM 分配为 **`TD-006`**）：M001 不校验图片源 `photo_id` 归属，防越权仅 M002 一道；当前无越权读取路径 → 风险**低**；建议纵深防御（M001 落源前预校验）或显式记录「单一防线」设计决定。
- **收口**：`CR-005` → **`Applied`**（8 条验收全通过）；`PROJECT_STATUS.md` → **`v0.24.0`**（v0.23.0 降基线）+ `更新日期 2026-09-16`；`TECH_DEBT.md` 更新日期 + 新增 `TD-006` + 「第二阶段需求（A1）」标记**已完成**。
- **⚠️ 过程失误（如实记录）**：切 `mock` 构造失败分支时，校验命令**误把 `.env` 全部 `AT_AI_*` 行（含三方 `sk-` 密钥）打印到对话**（`.env` 已 gitignore、未入库）→ **已建议用户轮换中转密钥**；后续同类操作改为**只打印目标行**。
- **临时资产已清理**：`.e2e/_tmp_t021_probe.py`、`_tmp_t021_fail.mjs`、截图 4 张、`backend/data/_tmp_t021.jpg`、`.pm_t021.xml`。
- **下一步**：`CR-006`（A → B → C）—— 即 `REQ-011` 余项；**优先处理轮换密钥**。

## `CR-006` 子项 A 开工：`TD-005` 窗口归属 —— 实施前核验 + 契约修订 + 签发任务书（用户：密钥暂不轮换，继续开工）

- **实施前核验（PM 读码，关键）**：
  - ⚠️ **CR 原假设不成立**：子项 A 设想「复用 M002 已有 `ensure_group` 通路拿到窗口上下文」，实测 `ensure_group(session, family_id, *, student_id, category, belong_date)` 的 **`belong_date` 是入参**（`m002/clients/task_client.py:358-368`）→ 上传时**无从得到**归属日。
  - ✅ **正解（M001 零改动）**：M001 契约**已有** `TaskQueryService.resolve_window(ts) -> WindowInfo`（`docs/modules/M001/MODULE_API.md` 内部服务接口表，**消费者已列 M002**；契约注明「纯计算，不锁配置」）；`WindowInfo = {belong_date, week_index, window_type, group_key}`（`m001/services/window_resolver.py:100-105`，含 4 点日界 + 周末合并 `W:<周五>`）。
  - ✅ **子项 B 的 `input_ref` 承载性核验**（CR 明文要求）：**可靠** —— `core/ai/service.py:208` 写 `input_ref={"photo_id": photo.photo_id, "candidate_count": …}`；存储为 `TEXT`(JSON)（`records.py:50`），**无独立列/索引** → 检索用 `json_extract(input_ref,'$.photo_id')` 或 LIKE，必要时加表达式索引。
  - ✅ **迁移机制**：`app/main.py:38` `Base.metadata.create_all` **不给既有表加列** → V1 无生产数据 → **重建库 + 重新种子**（`backend/data/*.db` 已 gitignore）；测试 fixture 新建库天然含新列。
  - 子项 C **待核验**（`link_service.py:1` 文件头已提「异步建议」，实施时确认调度形态）。
- **契约修订落地（M002 v0.4.2 → v0.4.3，6 文件）**：`MODULE_CONTRACT.md`（版本 + 非破坏性说明）/ `MODULE_API.md`（头部状态、`PhotoDTO` +`belong_date`/`group_key`、`API-M002-002` 响应 +2 可选字段 + 处理流程加「窗口归属解析」步、`API-M002-003` +2 可选过滤参数）/ `MODULE_DATA.md`（`photos` +2 列 +2 索引 + **派生冗余规则**：写入点唯一 = 上传流程、不随挂接/改归属日回填）/ `DATA_MODEL.md`（DATA-003 字段草案）/ `API_REGISTRY.md`（`API-M002-002`/`003` → v0.4.3）/ `MODULE_CHANGELOG.md`（**v0.4.3 + v0.4.2 回溯补登**）；`MODULE_REGISTRY.md` M002 → v0.4.3。
- **签发**：`docs/agents/Task-022.md`（`AGENT-M002` 实施，写区 = `m002/domain/models.py` + `repository/photo_repository.py` + `services/upload_service.py` + `clients/task_client.py` + `schemas.py` + 新增 `tests/integration/test_photos_window_belong.py`；**禁改** `m001/**`、`core/ai/**`、`frontend/**`、`docs/**`）与 `docs/agents/Task-023.md`（验收，8 条清单）。
- **Task-022 硬要求（PM 给定，避免自由发挥）**：① M002 **禁止复制归属规则**（单一知识源，违规打回）；② `resolve_window` 失败 → 落 `NULL` + warning，**上传必须 201 不阻断**；③ 过滤参数缺省不过滤（**向后兼容哨兵用例**）；④ `set_window_task` 语义不变（不得借机回填 `task_id`）；⑤ 用例须含**周末合并**（`group_key = W:<周五>`）与**不阻断**两类；⑥ 交付须给**迁移可复现命令**。
- **用户口径**：**密钥暂不轮换**（2026-09-16）→ 已记录，不再提示；本轮未触碰 `.env`（仅读改写过一次用于 mock 场景，已还原 `real`）。
- 待办：执行 `Task-022` → `Task-023` 验收 → 子项 A 收口（升版）；随后子项 B / C。

## `Task-022` 交付完成（`CR-006` 子项 A，用户：「重建可以」）

- **生产代码（8 文件，均在写区内）**：`m002/domain/models.py`（`Photo` +2 列 +2 索引）、`repository/photo_repository.py`（`create` +2 参 / `list_photos` +2 可选过滤）、`clients/task_client.py`（**新增 `WindowInfoRef`** + 协议 + `DefaultM001Gateway.resolve_window`（契约直调 + 存在性探针）+ `TaskClient.resolve_window`（**容错 → None + warning 不抛**））、`services/upload_service.py`（上传时解析并落归属）、`schemas.py` / `services/dto_builders.py`（`PhotoDTO` / `UploadPhotoOut` 各 +2 可选字段）、`services/photo_query_service.py` / `api/photo_routes.py`（+2 过滤参数）。
- **新增用例** `tests/integration/test_photos_window_belong.py` **6 例**：①参数化 周三/周五/周日（**周末合并** `W:2026-09-18`）+ 数据面实读 + `task_id is None`；②过滤 + **向后兼容哨兵**；③M001 不可用**不阻断**（替身，已标注）；④**单一知识源静态断言**（扫描 `m002/**` 不得含 `day_cutoff`/`week_index_for_date`/`f"W:` 等）。
- **时刻控制技巧（可复用）**：M001 归属引擎按**上传时刻**解析，故用例**冻结 `upload_service` 模块内的 `datetime` 名字**（`_FrozenDateTime(datetime)` + `monkeypatch.setattr(upload_service, "datetime", …)`）→ 精确断言 `belong_date`/`group_key`（含跨 4 点日界与周末合并）。**注意**：`install_fixed_window`（`app.state.window_resolver`）**不影响** `TaskQueryService.resolve_window`（后者走 `get_default_resolver()` 读全局 `Settings`）。
- **红→绿（PM 亲手）**：把上传时的 `window = TaskClient.resolve_window(...)` 改为 `window = None` → **4 例失败**（3 参数化 + 过滤，`assert [] == ['accaa96c-…']`，`RED_EXIT=1`）→ 还原 → 6 绿 → 全量 **`tests=273 failures=0 errors=0 skipped=0`、`GREEN_EXIT=0`**（267 + 6；`X..X` 哨兵仍 XPASS）。
- **迁移（用户批准重建）**：`start_server.ps1 -Stop` → `start_server.ps1 -Seed`（**官方重建入口**，内部调 `.e2e/seed.py` 重建 `acceptance.db` + 重新种子）→ `Seed OK.` + `Health OK`（PID 36488）。**真机复核原文**：`photos 含 belong_date=True group_key=True`、索引 `ix_photos_family_belong_date`/`ix_photos_family_group_key` 就位；上传 `201 / belong_date=2026-09-16 / group_key=2026-09-16`；**库内实读一致**且 `task_id=None`；过滤 `命中1 / 未命中0 / 缺省1`。
- **⚠️ 新登记 `BUG-008`（中，Confirmed，既有问题）**：迁移期间读日志发现 **DATA-009 在 SQLite 锁竞争下静默丢失** —— 真实 AI 调用 `POST …/chat/completions → 200`，但 `record_call` 遇 `sqlite3.OperationalError: database is locked` → `records.py:100-104` 仅 warning + `return None`（**无重试/无计数/无补偿**），调用方无从得知「本次未留痕」；**主链路不受影响**（照片上传 `201`、列表 200）。复现：`start_server.ps1 -Seed`（seed 脚本与服务并发写同库；SQLite 单写者 + pysqlite `timeout=5s`）。新建 `docs/changes/BUG-008.md`（现象/根因/影响/复现步骤/3 条推荐修复/验证要求/PM 复核结论），**只登记不修**；建议与 `CR-006` 子项 B 同批评估。
- **清理**：`.e2e/_tmp_t022_migrate_check.py`、`.pm_t022.xml` 已删除；服务运行中（8010）。
- **待办**：`Task-023` 验收（8 条）→ 子项 A 收口升版；随后子项 B（含 `BUG-008` 评估）/ C。
