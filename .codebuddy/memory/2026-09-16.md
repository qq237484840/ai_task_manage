# 2026-09-16

## `CR-005` / `CR-006` 批准 → 契约修订 + 签发 `Task-018`~`Task-021`（用户：「批准」）

- **用户批准**：`CR-005`（布置单图片解析通路）+ `CR-006`（A/B/C 三项全批）。PM 解读为整体批准（若仅部分批准需用户纠正）。
- **契约修订完成（`CR-005` §6 第一步）**：
  - `docs/modules/M001/MODULE_API.md` → **v0.3.0**：新增 `API-M001-022`（`POST /api/v1/tasks/{task_id}/reparse`）**详细契约**（含 §2.4 三情形：`placeholder` 写入 / `parsed` 整体替换 / 失败**不清空**；`confirmed` → `409 spec_confirmed`；失败不抛错）；新增内部回调槽「布置单图片源读取（M001 定义 / M002 注册实现）」；申请清单加 `M001-A5`；端点表 + 说明同步。
  - `docs/modules/M002/MODULE_API.md` → **v0.4.2**：内部服务接口 **+1** `provide_task_source_image(family_id, photo_id) -> TaskSourceImage | None`（`{mime, abs_path}`，归属校验、**路径不外发**）。
  - `docs/API_REGISTRY.md`：登记 `API-M001-022`（**Draft**，随实施转 Active）+ 顶部 2026-09-16 注释。
  - `docs/MODULE_REGISTRY.md`：M001 → **v0.3.0**、M002 → **v0.4.2**。
- **签发 4 份任务书**（`docs/agents/`）：
  - `Task-018`（`AGENT-M002`）：`provide_task_source_image` + `ensure_task_spec_image_provider_registered`（幂等 + 自愈 + **M001 未就绪不阻断**）+ 导入期注册 + `main.py` 启动期 **1 行**自愈；写区 = `m002/clients/task_client.py`、`main.py`、新增用例。
  - `Task-019`（`AGENT-M001`）：回调槽（与 `register_links_migration_hook` 同模式）+ `_to_ai_sources` 图片源转发 + `TaskService.reparse` + `API-M001-022` 路由 + **7 例（含防伪造红→绿）**；写区 = `task_parser.py` / 新 `image_provider.py` / `task_service.py` / `routers/tasks.py` / 用例。
  - `Task-020`（`AGENT-M001` 前端）：任务详情页「重新解析」按钮（`confirmed` 隐藏/禁用）+ `reparseTask`（可选 `timeoutMs=60000`，**不改** `http.ts`）+ **如实反馈**（无内容项不得谎报成功）。
  - `Task-021`（`AGENT-M002` 或 PM）：**只读验收** —— 8 条清单（纯图片 reparse 真实 Vision 产草稿 + DATA-009 `mock=0` / **防伪造红→绿** / `409` / §2.4 三情形 / provider 归属校验 / 全量回归 / 前端类型检查与构建 / 浏览器级），证据**分域**。
- **关键判据（已写入 `Task-019`）**：图片源转发**仅当 Vision Provider 为真实** —— 判据取 `core/ai/providers/registry.py::ResolvedProvider`（`degraded is False` **且** `reason == "real"`）；**禁止硬编码模型名**；Mock / degraded / 槽未注册 / 取图失败 → **不转发**（保持 `placeholder`，**不伪造** —— `BUG-004` 教训）。
- **`CR-006` 已批准待排期**（A：`photos` 加 `belong_date`/`group_key` + 窗口过滤；B：`link-suggestions` 新增 `last_attempt` 读 DATA-009；C：`POST /photos/{id}/link-suggestions/retry` → `202` + 轮询）。**须在 `CR-005` 任务链之后串行**（与 M002 `photo_repository`/`link_routes`/`schemas` 同文件冲突）。
- **下一步**：实施 `Task-018`（M002 提供方）→ `Task-019`（M001 消费 + 端点）→ `Task-020`（前端）→ `Task-021`（验收）；完成后 PM 置 `API-M001-022` → Active、`CR-005` → `Applied`。
