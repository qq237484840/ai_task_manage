# MODULE_REGISTRY —— 模块登记表

> 这是全项目最重要的导航文件之一。
> 维护：Project Master。模块划分/新增/变更时更新。
>
> 状态：**V1 已登记**（2026-09-08）。模块由「V1 MVP 总控 Agent 指令」§二 指定。需求基线已批准（**REQ-001~010**；`REQ-005`~`REQ-007` **Deferred（V2）**，其余 Approved）。**M001 Stable v0.1.2**（CHANGE-001 Applied，**契约 v0.2.0 Frozen —— 用户批准 2026-09-10；实施 = `Task-007`**）；**M002 Developing**（契约 v0.3.0 Frozen → **v0.4.0 Frozen（用户批准 2026-09-10）→ v0.4.1 Frozen（`CR-004` Applied，`API-M002-007` 响应体修订）**；**实施 = `Task-008`，已交付并经 PM 复核 APPROVED**）；**M003~M007 → Deferred**（`ADR-014`：M003/M004 职责吸收、M005~M007 后置 V2）。**V1 有效模块 = M001 + M002 + 共享/基础设施（`app/core/ai/`，执行方 `AGENT-AI`/Task-006）**。**`CHANGE-003` ② 契约修订评审已完成（契约定稿）；③ 实施全部收口（`Task-006`~`Task-010` 交付 + PM 复核 APPROVED）；④ 验收剧本 7 条可启动。**
> **变更执行完成（2026-09-08，v0.11.0）**：`CR-001`（任务 = 多学科作业登记单容器 + 学科作业段 `group_no`，PD-014/015）、`ACR-001`（两级主体：家庭账号家长 + 学生子账号，ADR-009/PD-016）、`CR-002`（**内容级判定链数据面重构**：布置登记拍照识别 + 逐题对齐判定 + 报告草稿复核定稿，ADR-010/PD-017~024）、`ACR-002`（**判定与 Provider 架构重构**：内容级主客观全判 + 真实三方默认，ADR-010/011）——**四项均 Approved（2026-09-08），合并 CHANGE-001 由 AGENT-M001 执行并经 PM 复核 APPROVED → Applied**：M001 冻结面随变更后实况定稿 **v0.1.2**（API-M001-013~017 登记 Active，本表 M001 行已同步）；**AGENT-M002 持 Task-002 编码 M002 v0.3.0**（任务书 `docs/agents/Task-002.md`）。**主线计划与验收里程碑按功能域 A~D 重排，见 `ROADMAP.md`（v0.9.0）。**
> **2026-09-10 变更（v0.13.0，CR-003 / ADR-013 / CHANGE-003）**：`CR-003` **Approved**（用户逐条确认）+ `ADR-013` **Accepted**（作业任务语义重构 + 归属与判定**双层模型**）→ 立项 **`CHANGE-003`（Executing）**，签发 **Task-004**（AGENT-M001，M001 契约 → v0.2.0 草案）与 **Task-005**（AGENT-M002，M002 契约 → v0.4.0 草案）——**二任务已于同日交付并经 PM 复核 APPROVED（v0.15.0 收口）**。**M001 冻结面被打开**（破坏性：唯一键 `(student_id, category, belong_date)`、`task_items` 废弃、子任务上移聚合层）。
> **2026-09-10 实施阶段启动（v0.16.0，③ 解禁）**：**M001 契约 v0.2.0 / M002 契约 v0.4.0 经用户批准 Frozen**（② 关闭）→ 签发 **`Task-007`**（AGENT-M001，M001 实施：事实层按天 → 归属引擎 → 聚合层 → 链路 T）与 **`Task-008`**（AGENT-M002，M002 实施：入口 `kind` → N:N 挂接 → 逐张复核 → 窗口级门控 → 完成分析；Task-002 冻结段增量改接）；**`Task-006`**（AGENT-AI，横切 `app/core/ai/`）**并行可启动**。
> **2026-09-10 契约修订收口（v0.15.0，CHANGE-003 ② 完成）**：**`Task-004`（M001 → v0.2.0 草案）/ `Task-005`（M002 → v0.4.0 草案）交付并经 PM 复核 APPROVED（两任务关闭）**；**API ID 已分配登记**（`API-M001-018~021` / `API-M002-007~011`，Draft）、**`DATA_MODEL` 字段级已同步**；**③ 实施与 `Task-006` 转为可启动**（契约正式 Frozen 待用户签署）。
> **2026-09-10 边界决议（v0.14.0，ADR-014，用户逐项拍板 CHANGE-003 §6 Q1~Q4）**：**M003 / M004 的 V1 职责被吸收** —— M003（AI 作业识别）→ 链路 T 归 **M001**、链路 H 挂接建议归 **M002** + 横切 `app/core/ai/`；M004（作业任务匹配）→ 聚合子任务级完成结论归 **M002**；**Module ID 保留不撤销，状态 → Deferred**。**M005 / M006 / M007 与 `REQ-005`~`REQ-007` 后置 V2**（状态 → **Deferred**）。横切 `app/core/ai/` **执行方 = 新增 `AGENT-AI`**（Task-006）。**V1 有效模块 = M001 + M002 + 共享/基础设施**（ADR-002「仅 7 个 Module ID、禁止提前实现 V2+」继续适用，本次为经批准的显式范围收窄）。
> **禁止任何 Agent 自行创建 Module ID**；新模块流程见 `DEVELOPMENT_GUIDE.md`。
>
> 别名说明：需求原文使用 `M01~M07`；本项目正式 Module ID 为 `M001~M007`（M01→M001，依此类推）。文档引用一律使用正式 ID。

## 模块登记表

| Module ID | 名称 | Domain | Purpose | 状态 | Owner Agent | 依赖 | 消费者 | Public APIs | 数据所有权 | 文档路径 | 版本 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| M001 | 作业任务管理 | 作业评定 | **按天**创建/维护作业布置记录（**事实层按天唯一 + 聚合层跨天**，随 CR-003），评定输入基础 | **Stable → 契约修订已完成 / 实施中**（v0.1.2 Stable / CHANGE-001 Applied；**契约 v0.2.0 Frozen（用户批准 2026-09-10）**；**实施 = `Task-007`（已完成，PM 复核 APPROVED）**） | AGENT-M001 | 无（基座） | M002, M005, M007 | `MODULE_API.md`（API-M001-001~012 Frozen + 013~017 Active + **018~021 Active（CR-003 新增）**） | DATA-001、DATA-002（含子账号）+ DATA-011（学校字典）+ **DATA-012/013/014/015（聚合层 / 判定单元 / 内容项 / 输入源，随 CR-003）** | `docs/modules/M001/` | v0.1.2 → **v0.2.0 Frozen** |
| M002 | 作业图片采集与归属 | 作业评定 | 上传/拍摄作业照片（**入口分「任务」/「作业」**）、质量检测、轻量归一、受控存储与**先采后认**归属（AI 挂接建议 + 逐张复核兜底，**N:N**，随 CR-003） | **Developing（③ 实施完成，PM 复核 APPROVED）**（契约 v0.3.0 Frozen → **v0.4.0 Frozen（用户批准 2026-09-10）→ v0.4.1 Frozen（`CR-004` Applied）**；**实施 = `Task-008`**，另 `Task-009` 前端「作业」域、`Task-010` 门控链路修复，Task-002 冻结段增量改接） | AGENT-M002 | M001（+CR-003 聚合层）；`app/core/ai/`（挂接 / 完成分析） | M005, M007 | `MODULE_API.md`（API-M002-001~006 + **007~011 Active（CR-003 新增；`API-M002-007` 响应体 v0.4.1 = `CR-004` Applied）**） | DATA-003（**修订**）+ **DATA-016/017（照片挂接 / 完成分析，随 CR-003）** | `docs/modules/M002/` | v0.3.0 → **v0.4.1 Frozen** |
| M003 | AI 作业识别 | 作业评定 | **任务布置单解析（链路 T）+ 作业照片挂接建议（链路 H）**（随 CR-003；**逐题识别作废**） | **Deferred**（V1 不实现；**职责并入 M001 + M002 + `app/core/ai/`**，`ADR-014` 决议 1；Module ID 保留，V2 回归时重评估） | ~~AGENT-M003~~（V1 不启用） | 横切 `app/core/ai/` | —（V1 无消费者） | —（V1 不设契约） | DATA-014/015/016（由 M001/M002 承载） | `docs/modules/M003/`（**V1 不创建**） | — |
| M004 | 作业任务匹配 | 作业评定 | **聚合子任务（学科）级完成结论**（完成/部分完成/未完成/无法判断 + 依据/置信度，ADR-013；**逐题对齐作废**） | **Deferred**（V1 不实现；**职责并入 M002**，`ADR-014` 决议 1） | ~~AGENT-M004~~（V1 不启用） | 横切 `app/core/ai/` | —（V1 无消费者） | —（V1 不设契约） | DATA-013/017（由 M002 承载） | `docs/modules/M004/`（**V1 不创建**） | — |
| M005 | AI 作业质量评价 | 作业评定 | **大模型综合评判**（六维质量 + 综合分，权重配置化）；**输入随 CR-003 改为聚合子任务级结论** | **Deferred（V2）**（`ADR-014` 决议 2；`REQ-005` 状态 Deferred） | ~~AGENT-M005~~（V1 不启用） | DATA-013/017（聚合结论）、`app/core/ai/` | —（V2：M006/M007） | —（V1 不设契约） | DATA-006（保留登记，V1 不写入） | `docs/modules/M005/`（**V2 回归时创建**） | — |
| M006 | AI 教师评价与改进建议 | 作业评定 | 可观察行为式评语 + 分级重写建议（**按聚合子任务组织**，随 CR-003） | **Deferred（V2）**（`ADR-014` 决议 2；`REQ-006` 状态 Deferred） | ~~AGENT-M006~~（V1 不启用） | M005 | —（V2：M007） | —（V1 不设契约） | DATA-007（保留登记，V1 不写入） | `docs/modules/M006/`（**V2 回归时创建**） | — |
| M007 | 今日作业综合报告 | 作业评定 | 聚合输出综合报告；草稿→家长复核定稿归档（PD-024）；**「今日」= 归属日、按聚合 / 周次组织**（随 CR-003） | **Deferred（V2）**（`ADR-014` 决议 2；`REQ-007` 状态 Deferred） | ~~AGENT-M007~~（V1 不启用） | M001, M002, M005, M006 | 用户（展示，V2） | —（V1 不设契约） | DATA-008（保留登记，V1 不写入） | `docs/modules/M007/`（**V2 回归时创建**） | — |

共享/基础设施（不属于业务模块，代码落地时建立，治理规则见 `DEVELOPMENT_GUIDE.md` §13）：
- **`app/core/ai/`（LLM 接入层，随 CR-003 新增）**：Provider 抽象（Vision/OCR/LLM）+ prompt 版本管理 + 结果 **schema 校验** + 降级；承接 ADR-011 与 DATA-009 统一调用记录。**执行方 = `AGENT-AI`**（`ADR-014` 决议 3，任务书 `docs/agents/Task-006.md`；**2026-09-10 起可启动，与 Task-007/Task-008 并行**）；**不设业务 Module ID**，写区仅 `backend/app/core/ai/**`
- AI Provider 抽象层（Vision/OCR/LLM + 统一调用记录 DATA-009）
- shared（通用 DTO/工具）、infrastructure（日志/异常/配置/存储）

## 模块 ID 规则

- 格式 `M001`、`M002`… 连续编号，不重复（规则见 `ID_GOVERNANCE.md`）
- **Module ID 一经分配不随意修改**；模块名可改
- 每个模块文档集存放于 `docs/modules/Mxxx/`，九件套清单见 `AGENT_GUIDE.md`
- V1 仅开放上表 **M001 + M002** 两个开发模块（+ 共享/基础设施）；**M003~M007 登记保留但状态 Deferred**（`ADR-014`）；任何新模块（含未来功能）须经 Project Master 走变更流程，禁止擅自扩展范围（ADR-002）

## 模块状态机

`Planned → Designing → Developing → Testing → Reviewing → Stable → Deprecated → Archived`

> **`Deferred`（本项目扩展态，见 `ADR-014`）**：已分配的 Module ID **不撤销、不复用**，但**不进入 V1 开发**（无契约、无代码、不创建 `docs/modules/Mxxx/`）；V2 回归时重新回到 `Planned` 并按 CR/CHANGE 流程推进。

## 模块拆分 / 合并判断

拆分触发：职责、生命周期、变化频率、数据所有权、权限、测试边界、部署需求、扩展方向明显不同。
合并触发：高度耦合、生命周期一致、职责重叠、数据边界无法分离、独立价值很低。
（V1 模块边界由需求 §二 指定；后续如需拆分/合并按上述标准评估并走变更流程，禁止仅因"文件太多"机械拆分）
